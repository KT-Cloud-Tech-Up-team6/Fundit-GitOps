apiVersion: v1
kind: Service
metadata:
  name: fundit-frontend-svc
  namespace: dev
  labels:
    app: fundit-frontend
spec:
  type: ClusterIP
  selector:
    app: fundit-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
