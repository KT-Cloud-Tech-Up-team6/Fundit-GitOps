apiVersion: v1
kind: Service
metadata:
  name: fundit-fulfillment-svc
  namespace: dev
  labels:
    app.kubernetes.io/name: fundit-fulfillment
    app.kubernetes.io/part-of: fundit
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: fundit-fulfillment
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
