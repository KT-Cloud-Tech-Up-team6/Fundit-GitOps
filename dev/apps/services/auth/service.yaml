apiVersion: v1
kind: Service
metadata:
  name: fundit-auth-svc
  namespace: dev
  labels:
    app.kubernetes.io/name: fundit-auth
    app.kubernetes.io/part-of: fundit
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: fundit-auth
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
