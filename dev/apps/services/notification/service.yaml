apiVersion: v1
kind: Service
metadata:
  name: fundit-notification-svc
  namespace: dev
  labels:
    app.kubernetes.io/name: fundit-notification
    app.kubernetes.io/part-of: fundit
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: fundit-notification
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
