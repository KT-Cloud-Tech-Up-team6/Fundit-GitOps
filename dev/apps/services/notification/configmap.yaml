apiVersion: v1
kind: ConfigMap
metadata:
  name: fundit-notification-config
  namespace: dev
  labels:
    app.kubernetes.io/name: fundit-notification
    app.kubernetes.io/part-of: fundit
data:
  KAFKA_BOOTSTRAP_SERVERS: kafka.kafka.svc.cluster.local:9092
  SPRING_FLYWAY_DEFAULT_SCHEMA: notification
  SPRING_FLYWAY_SCHEMAS: notification
  SPRING_FLYWAY_CREATE_SCHEMAS: "true"
  SPRING_JPA_PROPERTIES_HIBERNATE_DEFAULT_SCHEMA: notification
