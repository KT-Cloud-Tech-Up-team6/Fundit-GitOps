apiVersion: v1
kind: Service
metadata:
  name: fundit-project-svc
  namespace: dev
  labels:
    app.kubernetes.io/name: fundit-project
    app.kubernetes.io/part-of: fundit
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: fundit-project
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
