apiVersion: v1
kind: Service
metadata:
  name: fundit-order-svc
  namespace: dev
  labels:
    app.kubernetes.io/name: fundit-order
    app.kubernetes.io/part-of: fundit
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: fundit-order
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
