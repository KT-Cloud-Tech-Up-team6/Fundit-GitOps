apiVersion: v1
kind: Service
metadata:
  name: fundit-search-svc
  namespace: dev
  labels:
    app.kubernetes.io/name: fundit-search
    app.kubernetes.io/part-of: fundit
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: fundit-search
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
