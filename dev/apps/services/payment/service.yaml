apiVersion: v1
kind: Service
metadata:
  name: fundit-payment-svc
  namespace: dev
  labels:
    app.kubernetes.io/name: fundit-payment
    app.kubernetes.io/part-of: fundit
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: fundit-payment
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
