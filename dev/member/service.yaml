apiVersion: v1
kind: Service
metadata:
  name: fundit-member-svc
  namespace: dev
  labels:
    app.kubernetes.io/name: fundit-member
    app.kubernetes.io/part-of: fundit
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: fundit-member
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
