# Git-managed deployment image. Replace this placeholder after the ECR image exists.
# Use an ECR URI with a full commit tag (sha-<40 hex>, gateway-sha-<40 hex>)
# or an image digest (@sha256:<64 hex>). Do not put runtime secrets here.
GATEWAY_IMAGE=REPLACE_WITH_ECR_IMAGE_URI
